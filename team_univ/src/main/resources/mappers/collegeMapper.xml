<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.team.univ.persistence.CollegeDAO">

	<!-- 회원가입 - 부서정보 조회 -->
	<select id="selectCollege" resultType="com.team.univ.vo.CollegeVO">    
		SELECT * FROM college ORDER BY c_name DESC;
	</select>
	
	
	
</mapper>